# Runtime dependencies (see vtex_agent/*.py imports)
requests>=2.31.0
beautifulsoup4>=4.12.0
google-genai>=0.2.0
python-dotenv>=1.0.0

# Optional: only if google-genai is unavailable; gemini_mapper falls back to this package
# google-generativeai>=0.8.0
