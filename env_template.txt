# Copy this file to .env and fill in your values.

# --- Google Gemini (reporting / structure analysis) ---
# API key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Default in code if unset: gemini-2.0-flash (see vtex_agent/tools/gemini_mapper.py)
GEMINI_MODEL=gemini-2.0-flash
# Leave unset to use the default API endpoint for google-genai.
# Set only if your setup requires an explicit base URL (e.g. some Google AI Studio configs).
# GEMINI_BASE_URL=

# --- VTEX Catalog API ---
VTEX_ACCOUNT_NAME=your_account_name
VTEX_APP_KEY=your_app_key
VTEX_APP_TOKEN=your_app_token
# Optional: warehouse id for inventory updates (default in code: 1_1)
# VTEX_WAREHOUSE_ID=1_1

# --- GitHub (image hosting when not using --use-json-image-urls) ---
# Repo: "owner/repo" or full https://github.com/owner/repo URL
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_REPO=owner/repo_name
# Branch for uploads and raw.githubusercontent.com URLs (default: main)
GITHUB_BRANCH=main

# --- Optional: SKU specification selector assessment (OpenAI or Anthropic) ---
# If neither is set, the assessor step is skipped where the code allows it.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1-mini
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-sonnet-latest
